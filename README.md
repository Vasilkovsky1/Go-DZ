## Основные команды

- `git init` — инициализация нового Git-репозитория.
- `git status` — показать текущее состояние файлов.
- `git add .` — добавить все новые и измененные файлы из текущей директории в staging area.
- `git commit -m "message"` — создать коммит с сообщением.